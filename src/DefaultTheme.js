// @flow

import {
  indigo500,
  indigo700,
  pinkA200
} from './styles/colors';

const DefaultTheme = () => {
    return {
      primaryColor: indigo500,
      primaryDarkColor: indigo700,
      accentColor: pinkA200,
    };
};

export default DefaultTheme;
