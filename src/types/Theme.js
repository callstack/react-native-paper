/* @flow */

export type Theme = {
  colors: {
    primary: string;
    primaryDark: string;
    accent: string;
    text: string;
    secondaryText: string;
    disabled: string;
    divider: string;
  };
  fonts: {
    regular: string;
    medium: string;
    light: string;
    thin: string;
  }
};
