const path = require('path');

const docsRootDir = path.join(__dirname, '..', 'docs', 'components');
const libsRootDir = path.join(__dirname, '..', '..', 'src');

module.exports = {
  docsRootDir,
  libsRootDir,
};
